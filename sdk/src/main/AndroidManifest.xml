<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name="com.bonnie.vta.provider.VtaSdkInitProvider"
            android:authorities="\${applicationId}.vta-init"
            android:exported="false"
            android:initOrder="99" />

        <provider
            android:name="com.bonnie.vta.provider.VtaProvider"
            android:authorities="${applicationId}.vta"
            android:exported="true" />

        <service
            android:name="com.bonnie.vta.accessibility.VtaAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/vta_accessibility_service_config" />
        </service>
    </application>

</manifest>
